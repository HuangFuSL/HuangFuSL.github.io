\documentclass[standalone, version=2.0]{huangfusl-template}
\usepackage{tikz}
\usepackage{pgfplots}
\usepgfplotslibrary{fillbetween}
\begin{document}
    \begin{tikzpicture}
        \begin{axis}[
            axis lines=center,
            trig format plots=rad,
            enlargelimits=0.1,
            xlabel=$u$,
            ylabel=$t$
        ]
            \addplot+[no marks, variable=t, domain=0:2*pi, samples=100] ({- (2 * cos(t)^2 + sin(t) / (2 ^ 0.5)) + (-2 * cos(t) + 0.25 * sin(t)^2) + 1.5},{-(2 * cos(t)^2 + sin(t + pi / 4) / (2 ^ 0.5)) - (-2 * cos(t) + 0.25 * sin(t)^2)});
            % \path[name path=upperaxis] (axis cs:0.2,5) -- (axis cs:5,5);
            % \path[name path=loweraxis] (axis cs:0,5) -- (axis cs:-5, 5);
            % \addplot[name path=upper, domain=0:5, samples=50, thick] {1 / x};
            % \addplot[fill=gray!30] fill between [of=upperaxis and upper];
            % \addplot[name path=lower, domain=-5:0, samples=50, thick] {0};
            % \addplot[fill=gray!30] fill between [of=loweraxis and lower];
            % \addplot[thick] coordinates {(0, 0) (0, 5)};
            % \addplot[very thick, domain=-5:5, color=red] coordinates{(0, -5) (0, 5)};
        \end{axis}
    \end{tikzpicture}
\end{document}